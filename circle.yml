machine:
  node:
    version: 4.4

test:
  override:
    - npm run retire
    - npm test
    - nvm install 5.11 && npm rebuild && npm test
    - nvm install 5.12 && npm rebuild && npm test
    - nvm install 6.4 && npm rebuild && npm test
    - nvm install 6.5 && npm rebuild && npm test
