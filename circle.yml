machine:
  node:
    version: 4.6

test:
  override:
    - npm run retire
    - npm test
    - nvm install 5.11 && npm rebuild && npm test
    - nvm install 5.12 && npm rebuild && npm test
    - nvm install 6.6 && npm rebuild && npm test
    - nvm install 6.7 && npm rebuild && npm test
