machine:
  node:
    version: 4.0

test:
  override:
    - npm run retire
    - npm test
    - nvm install 4.1 && npm rebuild && npm test
    - nvm install 4.2 && npm rebuild && npm test
    - nvm install 4.3 && npm rebuild && npm test
    - nvm install 4.4 && npm rebuild && npm test
    - nvm install 5.0 && npm rebuild && npm test
    - nvm install 5.1 && npm rebuild && npm test
    - nvm install 5.2 && npm rebuild && npm test
    - nvm install 5.3 && npm rebuild && npm test
    - nvm install 5.4 && npm rebuild && npm test
    - nvm install 5.5 && npm rebuild && npm test
    - nvm install 5.6 && npm rebuild && npm test
    - nvm install 5.7 && npm rebuild && npm test
    - nvm install 5.8 && npm rebuild && npm test
    - nvm install 5.9 && npm rebuild && npm test
