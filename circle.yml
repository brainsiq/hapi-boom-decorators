test:
  override:
    - nvm install 4.0 && npm rebuild && npm run ci
    - nvm install 4.1 && npm rebuild && npm run ci
    - nvm install 4.2 && npm rebuild && npm run ci
    - nvm install 5.0 && npm rebuild && npm run ci
    - nvm install 5.1 && npm rebuild && npm run ci
